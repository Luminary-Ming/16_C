main : main.o swap.o
	gcc -o $@ $^
	#在 makefile 文件中可以引用变量, 需要使用 $ 进行引用
	# $@  目标文件(上一条语句 ':' 的左项)
	# $^  依赖文件(上一条语句 ':' 的右项)

clean :
	rm -rf *.o main
	# rm -rf $@ $^  ❌️ 因为 $@ 表示上一条语句 ':' 以左, 也就是 clean
	# $^ 表示上一条语句 ':' 以右, 也就是空